# Exemplo de variáveis de ambiente
# Copie para .env e ajuste os valores

# URL do Postgres (Neon ou outro). Formato:
# postgresql://USUARIO:SENHA@HOST:PORTA/NOME_BANCO?sslmode=require
DATABASE_URL=

# (Reservado para futuras chaves / integrações)
# API_KEY=
