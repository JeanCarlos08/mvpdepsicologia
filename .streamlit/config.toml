[server]
# Executa em modo headless e evita restrições CORS/XSRF para Cloud
headless = true
enableCORS = false
enableXsrfProtection = false

# Reload automático local (não afeta Cloud)
runOnSave = true

# Tamanho máximo de upload (MB)
maxUploadSize = 50

[browser]
gatherUsageStats = false
